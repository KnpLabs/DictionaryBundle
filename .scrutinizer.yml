---
build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run
  tests:
    override:
      -
        command: 'vendor/bin/phpspec run -n -fpretty'
        coverage:
          file: 'coverage.xml'
          format: 'clover'
